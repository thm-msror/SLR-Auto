Given details of this research paper, extract structured information about whether the paper is relevant (1-10, 10 being the most relevant) to building a pipeline where an LLM interprets user questions to guide retrieval of video clips using tagging, object detection, or audio-visual event detection, and then generates an answer based on retrieves clips.

Return your output strictly as a JSON object with the following fields DO NOT PUT COMMENTS:

{
  "review_paper": true/false

  "inclusion_criteria": {
    "Video retrieval/QA/semantic search": true/false/null if unsure,
    "Uses CV (detection, action recog, scene understanding)": true/false/null if unsure,
    "Uses Audio/ASR (speech, audio-visual events)": true/false/null if unsure,
    "Uses NLP/LLM for query or answers": true/false/null if unsure,
    "Has system/experiment with real video data": true/false/null if unsure,
    "Supports natural-language/semantic queries": true/false/null if unsure,
    "Multimodal fusion (vision+audio+NLP)": true/false/null if unsure,
    "Explicit semantic or multimodal retrieval": true/false/null if unsure
  },
  "exclusion_criteria": {
    "Metadata/keyword-only search": true/false/null if unsure,
    "Theoretical only, no implementation": true/false/null if unsure,
    "Focus on compression/storage/non-retrieval": true/false/null if unsure,
    "Non-English or no full text": true/false/null if unsure,
    "Pure review, no new empirical work": true/false/null if unsure
  }
  "included": true/false,

  "relevance": 10,
  "reason_of_relevance": "1–2 sentence summary of why this is or isn’t relevant",
  "key_technologies": [list of methods, models, or approaches mentioned],
  "modalities": [video, audio, text, multimodal],
  "task_type": "video QA / clip retrieval / event localization / other",
  "datasets": [if any],
  
  "application": [list of the types of videos was it applied on with results],
  "limitations": [list of limitations if any],
  "notes": "the highlight of this paper in 1-2 sentences"
}
