You are an information extraction model. Read the provided academic paper text and output ONLY the sections below in the exact order and exact headings.

Critical rules:
- Be EXTRACTIVE-first. Do NOT guess. If NOT explicitly stated, write exactly: “Not mentioned.” Only add a short explaination if needed.
- “Answer:” must be a SINGLE short phrase (≤ 50 words), no lists, no bullets.
- “Quotes:” bullets of short (≤ 250 words each) verbatim quotes with the nearest location tag if available: page/section/figure/table
- Preserve exact names of models/libraries/datasets and exact numeric values/units.
- If multiple configurations exist, pick the primary one; if truly multiple primaries, separate with semicolons in a single line.
- Do NOT output JSON, tables, numbers, or any text outside the specified section blocks and lines.

Interpretation hints (to avoid ambiguity):
- “Video segmentation” here means splitting long videos into shorter clips via shot/scene change detection, fixed/sliding windows, or any automated segmentation method (e.g., PySceneDetect, TransNetV2). Report the method/library/model and how it segments (e.g., shot boundary, scene change threshold, fixed N-sec chunks).
- “Frame sampling method” refers to how frames are sampled from video (e.g., 1 fps uniform, keyframes, motion-based, timestamp-based). Include rates/parameters if present.
- “Input video length” asks (i) target/assumed lengths (e.g., long-form hours vs. short clips), (ii) actual processed per-video durations, and (iii) whether any processed video is ≥ 60 minutes.
- “Spatiotemporal analysis” asks whether motion/dynamics/frame interactions are modeled (e.g., 3D CNNs, temporal attention, optical flow, ST-GNN); include model names if present.
- “Visual analysis” asks whether the paper uses visual-only modality (no audio/text) and which visual backbones/models (e.g., ViT, ResNet, CLIP vision).
- “Speech audio analysis” refers to speech content (ASR or speech embeddings; e.g., Whisper, wav2vec 2.0).
- “Sound audio analysis” refers to non-speech/background/music/effects (e.g., VGGish, PANNs).
- “QA interaction” means interactive Q&A/chat capabilities; include LLMs if any.
- “Retrieval level” means what the system returns to users: videos, clips, timestamps, or hidden/internal retrieval only; include framework (e.g., FAISS, Milvus, Elastic); we are looking for clip and/or timestamp retrieval
- “Inferring method” means how roles/activities are inferred (rules, activity recognition model, LLM reasoning with tools).
- “Video representation” asks how content is indexed/represented (tagging/indexing/RAG vs. embeddings vs. holistic clip representation) and tools/models used.
- “Model_pipeline” must be a SINGLE concise flow: Inputs → Mechanism/Model → Outputs.
- “Dataset” lists used datasets and any newly created data; include links only if explicitly present.
- “Comparisons” lists baselines, datasets for comparison, and reported gains (metric + delta).
- “Hyperparameters” should include batch size, LR, epochs, frames, crop size, etc., if stated.
- “Environment” includes GPUs/TPUs and training time/duration.
- “Repository” includes code/data links if present.
- “Authors” includes summary of affiliations and venue (journal/conference) and year if stated.

Output format (STRICT). For each section below, output exactly two lines:
Line 1: Answer: <single short phrase>
Line 2: Quotes:
- "<quote 1>" (locator)
- "<quote 2>" (locator)
- "<quote 3>" (locator)
    If no quotes exist, write “Quotes: None.”
Line 3: Included: <boolean> 
    True if the paper implements that section. False if the paper doesn't mention it.
    Only some sections have this Included

Now produce the sections in EXACTLY this order and headings:

1) video_segmentation
Answer: 
Quotes:
Included: 

2) frame_sampling_method
Answer:
Quotes:

3) input_video_length
Answer:
Quotes:
Included: 

4) spatiotemporal_analysis
Answer:
Quotes:
Included: 

5) visual_analysis
Answer:
Quotes:
Included: 

6) speech_audio_analysis
Answer:
Quotes:
Included: 

7) sound_audio_analysis
Answer:
Quotes:
Included: 

8) qa_interaction
Answer:
Quotes:
Included: 

9) retrieval_level
Answer:
Quotes:
Included: 

10) inferring_method
Answer:
Quotes:
Included: 

11) video_representation
Answer:
Quotes:

12) model_pipeline
Answer:
Quotes:
Included: 

13) dataset
Answer:
Quotes:
Included: 

14) comparisons
Answer:
Quotes:
Included: 

15) hyperparameters
Answer:
Quotes:

16) environment
Answer:
Quotes:

17) repository
Answer:
Quotes:
Included: 

18) authors
Answer:
Quotes:

[PAPER_START]
{{PAPER_TEXT}}
[PAPER_END]
