'''
change the log.py to log.json

use log.json to generate an md version of the logs so its easier to read 
add a table of contents 
the first section is the user input put the research_questions then the ones that have suggestion vs used. put them in a table with 1 row and 2 columns first column is suggest the second column is the used.

the second section is the fetched papers numbers then a table the titles, publisher, doi, and screening relevance_score
this section links to an appendix table that has a title columns then a columns fo 
'''