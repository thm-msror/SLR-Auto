# requirements.txt
# --- Core ---
python-dotenv==1.0.1
pandas==2.2.2
numpy==1.26.4
feedparser==6.0.10
ijson

# --- HTTP / Web ---
requests==2.31.0
playwright==1.43.0
beautifulsoup4==4.12.3

# --- APIs / LLMs ---
openai>=1.13.3

# --- Browser automation ---
selenium==4.36.0
webdriver-manager==4.0.2

# --- PDF to Markdown / Document Processing ---
marker-pdf==1.10.1
psutil>=7.1.0

# --- ML / OCR ---
torch==2.7.1
torchvision==0.22.1
torchaudio==2.7.1
transformers>=4.45.2,<5.0.0
surya-ocr==0.17.0

# --- Compatibility & Dev ---
protobuf==3.20.3
arxiv==2.1.0

# After installing requirements
# You must run this once (as you already noted):
#     pip install -r requirements.txt
#     playwright install
# This installs Chromium/WebKit/Firefox binaries that Playwright needs for institutional-auth flows (UDST proxy, IEEE, etc.).
# 
# Notes (important for your demo system)
# 
# Playwright → best choice for:
# - UDST proxy login
# - IEEE / Springer / ACM PDF auto-fetch
# - Handling cookies + sessions safely
# - Selenium kept as optional fallback (can remove later)
# - No credentials stored → session cookies live in memory only
# - Open-access papers → downloaded directly via requests
# - Institutional papers → downloaded via Playwright after proxy auth
