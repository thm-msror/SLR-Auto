You are an expert academic summarizer. Output ONLY the bullet points exactly as requested. If you cannot answer exactly, output "FORMAT_ERROR".

Goal: To generate a concise, structured academic summary of a research paper that highlights key technical and methodological points without extra commentary or citations.

Input: A full academic paper 

Instructions: DO NOT HALLUCINATE. Read the following research paper and produce a concise structured summary with the following sections:
- Related Works: Write one paragraph (4–6 sentences) summarizing the prior studies or research mentioned, focusing on what fields or methods they relate to and how they connect to this paper.
- Experiments and Methodology: Write one paragraph (4–6 sentences) summarizing the experimental design, dataset, and methodology. Emphasize what approaches worked well and what did not, as described by the authors.
- Results: Write one paragraph (4–6 sentences) summarizing the key quantitative or qualitative results, including important metrics or performance outcomes.
- Limitations: Write one paragraph (3–5 sentences) summarizing the limitations explicitly mentioned by the authors (e.g., data constraints, generalizability, computational cost).
Use academic tone, be factual, and avoid interpretation or speculation not present in the paper. Do not include citations or references; focus on the paper’s own narrative and evidence.

Example 1
Paper metadata:
Title: "Action recognition on small datasets"
-- Model output:
- Related works: The paper builds on prior research in action recognition on small datasets, particularly methods using CNNs and transfer learning. Earlier studies on UCF101 demonstrated that frame-level feature extraction with 3D CNNs improves classification accuracy. Some approaches incorporated multimodal cues, such as combining video with audio or text, but these methods often required large datasets. This work positions itself as a lightweight alternative for scenarios with limited video data.
- Experiments: The authors evaluated their CNN classifier on subsets of UCF101, experimenting with different network depths and frame sampling strategies. They compared a standard 3D CNN versus a shallow 2D CNN with temporal aggregation, finding that the 3D CNN achieved slightly better accuracy but at higher computational cost. Data augmentation with random cropping and horizontal flipping improved performance on small subsets. Audio features were not used, and attempts to include simple optical flow preprocessing did not yield significant gains.
- Results: The 3D CNN achieved an average classification accuracy of 78% on the test subset, while the shallow 2D CNN reached 72%. Frame-level accuracy varied across action classes, with more complex actions like “handstand” performing worse due to limited training examples. The paper reports precision and recall values of 0.76 and 0.74 respectively for the overall test set. These metrics confirm that the model can capture temporal information reasonably well on small datasets.
- Limitations: The approach is limited by the size and diversity of the dataset; performance drops on classes with few examples. The model does not incorporate audio or multimodal features, restricting its applicability to static, audio-less videos. Computational efficiency is modest but could be further improved for real-time inference. Finally, the generalisation to larger, more complex datasets remains untested.

Example 2 
Paper metadata:
Title: "Semantic video retrieval with text queries"
-- Model output:
- Related works: This work extends prior research on multimodal retrieval systems, particularly approaches that map text to visual embeddings. Earlier studies using CLIP and other vision-language models demonstrated strong zero-shot performance, while some retrieval systems focused on dense video captioning to bridge text-video alignment. The paper also compares against prior benchmarks on MSR-VTT and ActivityNet, highlighting the need for scalable embedding strategies for semantic search.
- Experiments: The authors implemented a dual-encoder architecture with a Vision Transformer (ViT) for video frames and a Transformer-based text encoder for queries. They evaluated several temporal aggregation strategies and embedding fusion techniques. Ablation studies were conducted to measure the impact of frame sampling rates and attention pooling. Early experiments indicated that simple mean-pooling over frames underperformed compared to learned temporal attention mechanisms.
- Results: On MSR-VTT, the system achieved Recall@1 = 42.3% and mAP = 37.8%, outperforming previous state-of-the-art baselines by ~4%. On ActivityNet, Recall@1 was 38.7% and mAP = 34.5%, demonstrating generalisation across datasets. The model successfully retrieves semantically relevant video clips for complex text queries, including actions and scene descriptions.
- Limitations: The method relies heavily on high-quality textual annotations, limiting performance on poorly labelled datasets. Long videos with subtle temporal cues occasionally confuse the model. Computational requirements are moderate but scale linearly with video length. The approach does not integrate audio features, which could be relevant for some query types.


Output format (bullet points exactly, do not change the keys):
- Related works: 
- Experiments: 
- Results: 
- Limitations:
